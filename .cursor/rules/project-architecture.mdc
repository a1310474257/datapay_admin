---
description: DataPay Admin 项目架构与目录约定（始终应用）
alwaysApply: true
---

# DataPay Admin 项目架构

## 技术栈

| 层次 | 技术 |
|------|------|
| 框架 | Vue 3 + Vite 5 |
| UI 库 | Element Plus（中文 `zhCn` 语言包） |
| 状态管理 | Pinia |
| 路由 | Vue Router 4（HTML5 History 模式） |
| 图表 | ECharts 6 |
| 样式 | SCSS（scoped 组件样式） |
| 图标 | @element-plus/icons-vue（全局注册） |

## 目录结构

```
datapay-admin/src/
├── assets/
│   └── styles/index.scss   # 全局样式与 Element Plus 覆盖
├── components/             # 可复用公共组件
├── layouts/
│   └── AdminLayout.vue     # 后台主布局（侧边栏 + 顶栏 + 内容区）
├── router/
│   └── index.js            # 路由配置与导航守卫
├── stores/
│   └── user.js             # 用户认证状态（token + userInfo）
└── views/                  # 页面视图，按模块分目录
    ├── login/index.vue
    ├── dashboard/index.vue
    ├── users/              # 用户管理
    ├── transactions/       # 交易记录
    ├── bills/              # 账单管理
    ├── withdraw/           # 提现申请
    ├── reports/            # 数据报表
    └── settings/           # 系统设置
```

## 命名约定

- 视图文件：`views/<模块>/index.vue`，模块名用小写连字符（kebab-case）
- 组件文件：大驼峰命名，如 `TransactionTable.vue`
- Store 文件：小驼峰，如 `user.js`、`transaction.js`
- 路由 `name`：大驼峰，如 `Users`、`TransactionList`

## 开发服务器

- 端口：`5190`，host `0.0.0.0`
- 路径别名：`@` → `src/`

## 业务模块划分

- **用户管理**：用户列表（/users）、角色权限（/roles）
- **财务管理**：交易记录（/transactions）、账单管理（/bills）、提现申请（/withdraw）
- **数据报表**：日报（/reports/daily）、月报（/reports/monthly）
- **系统设置**：/settings

## 其他要求
-生成的代码要加上详细的中文注释